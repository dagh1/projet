{% extends 'base.html.twig' %}

{% block title %} Gestion Universite{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-content">
                    <h4 class="card-title">Liste des Universites</h4>
                    <a href="{{ path('admin_universite_ajouter') }}" class="btn btn-success"><i class="material-icons">control_point</i>
                        Ajouter</a>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                            <tr>
                                <th class="col-lg-3">Nom</th>
                                <th class="col-lg-2">Email</th>
                                <th class="col-lg-2">siteweb</th>
                                <th class="col-lg-2">Telephone</th>
                                <th class="col-lg-1">Ville</th>

                                <th class="text-right col-lg-1">Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for Universite in Universites %}
                                <tr>
                                    <td>{{ Universite.nom|capitalize }}</td>
                                    <td>{{ Universite.email }}</td>
                                    <td>{{ Universite.siteWeb }}</td>
                                    <td>{{ Universite.telephone }}</td>
                                    <td>{{ Universite.ville.nom|capitalize }}</td>
                                    <td class="td-actions text-right">
                                        <a href="{{ path('admin_Universite_modifier', {'id': Universite.id}) }}" rel="tooltip"
                                           class="btn btn-success btn-simple"
                                           data-original-title="" title="">
                                            <i class="material-icons">edit</i>
                                        </a>
                                        <a href="{{ path('admin_Universite_supprimer', {'id': Universite.id}) }}"
                                           rel="tooltip" class="btn btn-danger btn-simple"
                                           data-original-title="" title="">
                                            <i class="material-icons">close</i>
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
